name: Perf Pipeline
on:
  workflow_run:
    workflows: ["CI/CD Pipeline"]
    types: [completed]
    branches: [main]
jobs:
  perf:
    if: github.event.workflow_run.conclusion == 'success'
    uses: shermanlye-ideagen/demo-cicd-platform/.github/workflows/perf-pipeline.yaml@main
    with:
      product_name: "lucidity"
    secrets: inherit
